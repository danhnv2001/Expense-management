<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.expensemanagement">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseManagement"
        tools:targetApi="31">

        <!-- LoginActivity làm Activity chính (Launcher Activity) -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Các activity khác -->
        <activity
            android:name=".StatsThangNay"
            android:exported="false" />

        <activity
            android:name=".StatsNamNay"
            android:exported="false" />

        <activity
            android:name=".StatsHomNay"
            android:exported="false" />

        <activity
            android:name=".StatsActivity"
            android:exported="false" />

        <activity
            android:name=".CaiDatActivity"
            android:exported="false" />

        <activity
            android:name=".ListGiaoDichMainActivity"
            android:exported="false" />

        <activity
            android:name=".WelcomeActivity"
            android:exported="false" />

        <activity
            android:name=".ThuChiMainActivity"
            android:exported="false" />

        <activity
            android:name=".ThemgiaodichActivity"
            android:exported="false" />

        <!-- MainActivity không còn làm Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false">
        </activity>

    </application>

</manifest>
